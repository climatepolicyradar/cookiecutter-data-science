{
    "project_name": "project_name",
    "repo_name": "{{ cookiecutter.project_name.lower().replace(' ', '_') }}",
    "description": "A short description of the project, to be added automatically to the readme and used in setup.py.",
    "s3_path": "[OPTIONAL] bucket-for-syncing-data/folder (do not include 's3://'). `folder` is optional.",
    "aws_profile": "default"
}
